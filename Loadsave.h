#IFNDEF 'Loadsave.h'
#DEFINE 'Loadsave.h'

void save();

  /*class Save {
    private:
      string name = "Unknown"
      string level = "Terrestrial"
      unsigned int energy = 15;
      int steps = 0;
    public:
      Save();
      Save(string n, string l, unsigned int e, int s);
      Save(Save& in_obj);
  } */

#ENDIF
